<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
  <li>
    <%= link_to tag.name, tag_url(tag) %>
    <%= link_to "delete", tag_url(tag), method: :delete, data: {confirm: "Really delete '#{tag.name}' tag?"} %><br />
  </li>
  <% end %>
</ul>
